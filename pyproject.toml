[project]
name = "email-env-ai"
version = "0.1.0"
description = "Email triage OpenEnv environment"
authors = [
    {name = "Khushi"}
]
dependencies = [
    "fastapi",
    "uvicorn",
    "pydantic"
]

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
